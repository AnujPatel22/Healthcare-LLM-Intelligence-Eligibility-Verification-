# Codex Prompts

Use these prompts to extend the project:

- Add Redis caching for RAG search responses while preserving the mock fallback path.
- Add a k6 benchmark script that measures `/claims/{claim_id}/validate` and `/rag/search`.
- Add rule versioning with effective dates and a frontend filter.
- Add OpenTelemetry traces for eligibility, RAG retrieval, validation, and explanation generation.
