# Benchmark Plan

The included benchmark endpoint simulates the fast validation and cached retrieval path.

Targets:

- 20,000+ synthetic queries/day.
- Sub-200ms p95 for cached validation/retrieval.
- 94% synthetic rule-match accuracy.
- 80%+ cache hit rate.
- 3 API replicas for the baseline target.

These metrics are synthetic demo values unless replaced by real load-test output.
